indoor.py:
input to output in lowercase. 
Punctuation and whitespace should be outputted unchanged. 

playback.py:
input to outputs, replacing each space with ... (i.e., three periods).

faces.py:
convert :) to 🙂 and :( converted to 🙁

einstein.py:
mass as an integer (in kilograms) and then outputs the equivalent number of Joules as an integer

tip.py:
calculating percentage on overall value
  

